# 手机访问与全流程演练

当前参与者地址使用 `http://10.168.187.118:8765`。电脑和手机必须在可互通的同一局域网内，电脑保持开机。刷新工作台后重新下载海报或生成二维码；以前包含 localhost 的海报不会自动更新。

先在手机浏览器访问上述地址。若仍不能打开，需要检查 Windows/WSL 防火墙、Wi-Fi 客户端隔离；换同一热点也可排查。不要关闭整个防火墙。手机打开登录页即说明网络通了；参与者应扫码进入 `/join/<活动ID>`，不需要负责人密钥。

更换 Wi-Fi 后 IP 可能改变，需要更新服务启动参数 `--public-base-url`。正式跨网络使用应部署到具有 HTTPS 域名的服务器，不要直接分享负责人访问密钥。启动参数 `--host` 控制监听网卡，`--public-base-url` 统一控制分享链接、二维码和海报地址。

## 真实演练

当前网络链路为 Windows `10.168.187.118:8765` / `127.0.0.1:8765` → WSL `127.0.0.1:18766`。Windows 使用 `netsh interface portproxy` 转发，依赖 `iphlpsvc` 服务运行。后端内部端口不向局域网开放；Windows 的 `CampusEvents-LAN-8765` 规则仅允许 `10.168.0.0/16` 来源访问 8765。更换 IP 时还需同步更新转发监听地址和相应防火墙来源范围。服务启动覆盖配置位于 `/run/systemd/system/campus-events-mvp.service.d/lan.conf`，是本次运行的临时配置，不保证重启电脑后保留。

Windows 管理员 PowerShell 可用 `netsh interface portproxy show all` 查看转发。撤销本次转发可分别执行 `netsh interface portproxy delete v4tov4 listenaddress=10.168.187.118 listenport=8765` 和 `netsh interface portproxy delete v4tov4 listenaddress=127.0.0.1 listenport=8765`。撤销后需重新配置后端监听，原入口将不可用。

新建一场名称含“演练”的活动，不复用正式活动。独立 Agent 测试页只测试示例，不会产生真实报名和签到。

1. 电脑作为负责人，填写真实演练时间（例如 10 分钟后）、10 人容量及小额预算，生成策划并确认。生成宣传并确认后，才开放报名。
2. 用三部手机或不同浏览器分别作为参与者 A、B、C，使用不同测试邮箱报名。A 提问“需要带电脑吗”，B 填写无障碍座位需求，C 不填需求。保存各自报名凭证；同一浏览器同一活动默认只保留一张凭证。
3. 负责人刷新报名名单，运行报名分析，为 A 生成并确认答复。手机在“我的报名”中刷新查看答复。
4. 负责人点击“开启现场签到”。A、B 在手机“我的报名”中刷新并签到，C 保持未签到，以产生真实缺席数据。
5. A 在“参与反馈”提交“希望增加讨论时间”，评分 3；B 提交“案例有帮助”，评分 5。负责人在现场 Agent 中输入“嘉宾预计晚到 20 分钟”，查看建议，人工决定采纳或拒绝。建议不会自动修改日程。
6. 负责人结束活动；已签到的 A、B 再提交活动后反馈。每人在现场和活动后各可提交一次；每阶段不可重复提交。
7. 生成复盘，对照真实报名 3 人、到场 2 人、到场率约 66.7% 核验报告；反馈人数按参与者去重，不等于反馈条数。确认复盘后生成总结，确认后归档。

还可用独立演练活动测试：重复邮箱报名、容量已满、未开场签到、未签到提交反馈、已归档后报名。这些操作应被拒绝，正式数据不应被改坏。

当前扫码入口和凭证签到不验证 GPS、现场身份或防代签；这是流程演练，不是防作弊验收。活动时间线按设备真实时间计算，不提供时间加速。AI 内容由真实 Codex 生成；报名、签到、反馈由真实 HTTP 接口写入数据库，负责人仍承担批准操作。
